{
  "name": "zodiac-little-space",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync": "bash scripts/sync-assets.sh",
    "predev": "bash scripts/sync-assets.sh",
    "prebuild": "bash scripts/sync-assets.sh",
    "dev": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview --host"
  },
  "dependencies": {
    "astro": "^7.0.6",
    "pinyin-pro": "^3.28.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
